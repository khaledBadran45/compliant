import { ColorWDirective } from './color-w.directive';

describe('ColorWDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorWDirective();
    expect(directive).toBeTruthy();
  });
});
